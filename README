CloudKeyring is a simple encrypted DB manager for passwords stored in cloud services.
It operates by monitoring encrypted files in a directory for changes and reloading and decrypting their contents after such changes.